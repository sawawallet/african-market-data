[package]
name = "amd-feed"
description = "Direct exchange feed handlers (MITCH, ITCH/MoldUDP64). Requires a market data licence; empty in the public repository."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
amd-core = { workspace = true }
